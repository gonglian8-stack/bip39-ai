# 内容更新流程（Guides）

目标节奏：每月 2–4 篇，质量优先。不做每日更新，不批量生成近似文章。

## 选题

1. 来源优先级：GSC 实际查询 > `KEYWORD-MAP.md` > 用户真实问题。
2. 先过 `PRODUCT-PLAN.md` §11 决策门槛：独立任务？与现有页差异？能否提供可核验价值？会不会和现有页抢同一意图？
3. 如果答案其实属于现有页面，改为补充那一页的章节或 FAQ，而不是新建文章。
4. 永不承接：破解、找回缺词、暴力破解、“恢复服务”类意图。

## 写作要求

- 首段直接回答问题（1–2 句，同时作为 `summary`）。
- 技术事实以 BIP39 规范、测试向量或本站测试结果为准；数字要能复算。
- 链接到相关工具页，并在文末放安全提示（不在网站输入真实助记词）。
- 不写收益、价格、钱包排名；提到具体产品必须有可核验来源，联盟链接必须标注。

## 发布步骤

1. 新建 `src/pages/guides/<slug>.md`，复制现有文章的 frontmatter 并填写。
2. 修改已有文章时填写 `updated` 日期。
3. `npm run build && npm run check:dist`（自动加入 /guides/、sitemap、llms.txt，并检查标题/描述长度与链接）。
4. 在 `src/data/changelog.ts` 记一条。
5. 部署后在 GSC 提交该 URL 检查。
