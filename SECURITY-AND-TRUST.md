# BIP39.ai 安全与信任要求

## 1. 威胁模型

需要防范或明确告知的风险包括：

- 服务器或 CDN 返回被篡改代码；
- 第三方脚本读取输入；
- 浏览器扩展、恶意软件、剪贴板和屏幕记录；
- 分析、错误上报或日志意外采集秘密；
- 用户把“本地运行”误解为绝对安全；
- 钓鱼克隆站借用品牌；
- 构建产物和源码不一致；
- AI/客服被用户主动发送秘密。

## 2. 数据分类

### 永不收集或发送

- mnemonic/recovery phrase；
- entropy；
- seed；
- private key；
- BIP39 passphrase；
- 完整派生地址集合；
- 可以重建上述秘密的数据或哈希指纹。

### 可匿名统计

- 页面 ID；
- 工具启动与完成；
- 错误类型枚举，不含原始输入；
- 离线下载点击；
- 设备类别与聚合来源。

## 3. 前端要求

- 核心密码学计算不发起网络请求；
- 敏感输入不进入 URL、localStorage、sessionStorage、IndexedDB 或 cookie；
- 不使用会话回放、热图和录屏；
- CSP 禁止非白名单脚本和外连；
- 不在工具页加载广告、客服聊天或社交 embed；
- 错误上报必须先做输入隔离与测试；
- 浏览器自动填充和拼写检查按字段风险合理关闭；
- 清空按钮立即重置界面状态；
- 不承诺 JavaScript 能可靠擦除所有浏览器/系统内存痕迹。

## 4. 离线发行要求

- 版本化 standalone HTML；
- 发布 SHA-256；
- 源码 tag 与构建步骤；
- 尽可能实现可复现构建；
- 测试文件在断网环境正常运行；
- 独立检查无外部字体、CDN、analytics、API 和动态 import；
- 每个 Release 附测试向量结果和变更日志。

## 5. 内容与品牌要求

- 不使用“100% safe”“unhackable”“audited”等未被证实的词；
- “client-side”必须能从网络检查和源码得到验证；
- “open source”必须链接实际仓库和许可证；
- “security audit”必须提供审计方、范围、版本、日期和报告；
- 每个工具说明适合测试/教育还是可离线处理真实材料；
- 任何联盟关系清楚披露。

## 6. 测试要求

- 官方 BIP39 test vectors；
- 至少一个独立库交叉验证；
- 所有支持词数的有效/无效 checksum；
- Unicode NFKD、日文空格和 passphrase 边界测试；
- 随机源失败状态；
- 无网络模式；
- CSP 与第三方请求检查；
- 日志、分析和错误上报泄漏测试；
- 键盘、屏幕阅读器和移动端测试。

