# BIP39.ai 产品规划书

## 1. 项目定义

### 1.1 一句话产品

BIP39.ai 是一个面向全球英语用户的 BIP39 工具与知识中心，提供可验证、开源、客户端运行并可下载离线使用的生成、校验、转换、词表和校验和工具。

### 1.2 用户为什么搜索

用户不是为了“浏览一个加密网站”，而是要立即完成某个任务：

1. 生成符合 BIP39 的测试助记词；
2. 判断助记词结构和 checksum 是否有效；
3. 查询官方 2048 词表；
4. 在 entropy、mnemonic 与 seed 之间转换；
5. 理解 checksum、passphrase 和词数规则；
6. 下载一个可以断网运行的可信工具。

### 1.3 产品定位

```text
可信 BIP39 工具
+ 离线优先安全模型
+ 可核验的标准解释
+ 开发者友好的测试向量和下载
= BIP39.ai
```

### 1.4 不做什么

首版明确不做：

- 钱包、托管、交易、余额查询或资产扫描；
- 云端保存助记词、Seed、私钥或 passphrase；
- 暴力破解、缺词找回、钱包破解器或“恢复服务”；
- 远程 mnemonic-to-private-key API；
- 账号系统、社区、用户公开结果；
- 为了域名而加入读取用户秘密的 AI 聊天；
- 未验证需求就批量建立币种、钱包或语言薄页；
- 展示广告、第三方营销脚本或工具输入行为回放。

## 2. 目标市场和用户

### 2.1 首发市场

- 国家：先以全球英语搜索为产品口径；上线前需在关键词工具中补充美国数据作为主要判断基准。
- 语言：首版只做英文正式页面。
- 中文：作为后续独立本地化实验，不在首版中做机器翻译镜像。

### 2.2 用户角色

#### A. 立即使用工具的用户

- 想生成测试 mnemonic；
- 想验证格式或 checksum；
- 想把 entropy 与 mnemonic 相互转换；
- 最看重：立即可用、隐私、正确性、离线下载。

#### B. 学习 BIP39 的用户

- 想理解 12/24 words、checksum、entropy、passphrase；
- 最看重：清楚示例、公式、权威来源、可交互演示。

#### C. 开发者

- 想找 wordlist、test vectors、代码库或嵌入组件；
- 最看重：可复制代码、确定性输出、版本、许可证、测试与下载。

## 3. 核心产品原则

### 3.1 一个独立任务对应一个主页面

近义词由同一页面自然覆盖，不执行“一个关键词复制一页”。只有任务、输入、输出或所需内容显著不同才拆页。

### 3.2 工具和落地页合并

每个工具 URL 同时包含：

1. H1 与一句准确承诺；
2. 可立即使用的测试/演示模式；
3. 离线与隐私说明；
4. 示例输入输出；
5. 使用方法；
6. 原理、限制和权威来源；
7. FAQ；
8. 相关工具和教程内链；
9. 自然的下载或商业出口。

### 3.3 Offline-first，而非口头宣称 client-side

公开网页可以运行演示和测试向量。真实钱包材料应引导用户下载离线单文件版本、核对哈希、断网后使用。HTTPS 只证明传输加密，不等于网站可信。

### 3.4 正确性优先于页面数量

密码学结果必须通过官方/公开测试向量和至少一个独立实现交叉测试。内容必须区分规范事实、常见行业叫法和建议。

## 4. 首版产品范围

### 4.1 首版可索引页面

```text
/
/bip39-generator/
/bip39-validator/
/bip39-word-list/
/bip39-checksum/
/bip39-converter/
/bip39-passphrase/
/bip39-offline/
/what-is-bip39/
/security/
/privacy/
/open-source/
```

其中前九个是获客/任务页面，后三个是信任页面。

### 4.2 首版工具能力

- Generator：12/15/18/21/24 words；默认英文；测试用途；显示 entropy 与 checksum 结构。
- Validator：词数、词表、语言候选、checksum；清楚区分“结构有效”和“钱包安全”。
- Word List：英文 2048 词、序号、搜索、TXT/JSON/CSV 下载和官方来源。
- Checksum：可视化 ENT/CS/MS；只用示例或用户自行生成的测试数据。
- Converter：entropy ↔ mnemonic、mnemonic → seed 的测试模式；不显示私钥。
- Passphrase：交互演示同一 mnemonic 加不同 passphrase 得到不同 seed；使用固定测试向量。
- Offline：单文件下载、SHA-256、版本、构建说明和断网使用步骤。

### 4.3 暂缓到第二阶段

- 多语言 wordlists；
- last word calculator；
- BIP32/BIP44/BIP49/BIP84/BIP86 派生路径；
- 钱包兼容性页面；
- JavaScript/Python/Rust 开发教程；
- 桌面应用、SDK、白标与 API。

只有关键词数据、GSC 查询或真实用户请求支持时才进入第二阶段。

## 5. 用户流程

### 5.1 工具查询

```text
Google 查询 bip39 validator
→ /bip39-validator/
→ 首屏立即看见输入/测试示例
→ 明白在线与离线边界
→ 用测试数据完成校验
→ 如涉及真实秘密，下载离线版并核对哈希
→ 阅读 checksum 或 word list 相关内容
```

### 5.2 知识查询

```text
Google 查询 what is bip39
→ /what-is-bip39/
→ 首屏得到简短定义
→ 查看 entropy → mnemonic → seed 图解
→ 进入 generator/checksum/passphrase 交互演示
```

### 5.3 商业流程

```text
免费工具/教程
→ 建立信任并完成任务
→ 离线下载、开发者组件或安全硬件指南
→ 联盟成交或 B2B 线索
```

## 6. 商业模式

### 6.1 第一阶段：相关产品联盟

只在与任务自然相关的内容中推荐：

- 硬件钱包；
- 金属助记词备份产品；
- 离线签名或备份设备。

要求：清楚标注 affiliate；不把推荐伪装成安全排名；工具输入区不出现广告或第三方脚本。

### 6.2 第二阶段：开发者产品

- 可自托管 BIP39 Widget；
- 经过测试的 SDK 包装与长期支持；
- 企业白标离线工具；
- 测试向量、兼容性数据和批量导出。

### 6.3 不作为主模式

- 展示广告：与安全信任冲突；
- 消费者月订阅：任务低频，复购假设弱；
- 云端助记词 API：安全边界错误；
- 代客恢复或破解：滥用、合规和品牌风险过高。

## 7. SEO 产品架构

### 7.1 信息架构

```text
Home
├── Tools
│   ├── Generator
│   ├── Validator
│   ├── Converter
│   └── Checksum
├── Reference
│   ├── Word List
│   ├── Passphrase
│   └── What Is BIP39
├── Offline
└── Trust
    ├── Security
    ├── Privacy
    └── Open Source
```

### 7.2 内链规则

- 首页链接所有首版核心页面；
- 工具页链接原理页、离线页和最相关的两个工具；
- 原理页在解释出现处上下文链接对应工具；
- 每页使用普通 `<a href>`、面包屑和规范 URL；
- 不制作全站关键词密网，不用 `title` 属性隐藏补词；
- 所有可索引页面必须至少有一个正常内部入口。

### 7.3 技术 SEO 基线

- 静态生成或 SSR，关键正文存在于初始 HTML；
- 每页唯一 Title、Description、H1 和 canonical；
- 统一尾斜杠规则；
- Sitemap 只含规范、200、可索引页面；
- robots 不阻止核心页面和必要资源；
- 正确 404、重定向、Open Graph、favicon；
- 结构化数据只使用页面真实可见内容，不虚构评分；
- 移动端、键盘、可见焦点、label、错误播报合格；
- 第三方资源最小化，Core Web Vitals 以真实数据验收。

## 8. GEO 与可信内容

- 关键定义用短段落直接回答；
- 标准事实附近链接官方 BIP39 规范和实现来源；
- 显示最后核验日期和工具版本；
- 提供原创交互图解、测试结果和兼容性说明；
- 明确“BIP39 不等于 BIP32/BIP44”；
- 作者/维护者、项目仓库、安全模型保持一致；
- 不为 AI 搜索批量生产近似问答页。

## 9. 数据与分析

### 9.1 允许采集

- 页面浏览（不含输入内容）；
- 工具启动事件；
- 测试示例载入；
- 操作成功/错误类别的匿名计数；
- 离线下载点击；
- 外链点击；
- 页面语言、设备和来源的聚合数据。

### 9.2 禁止采集

- 输入值；
- 单词数量与具体词的组合指纹；
- Seed、entropy、passphrase、派生结果；
- DOM 回放、输入框录屏、剪贴板内容；
- 可逆推出用户秘密的错误日志。

### 9.3 核心指标

SEO 漏斗：

```text
发现 → 抓取 → 索引 → 曝光 → 点击
```

产品漏斗：

```text
页面访问 → 工具启动 → 合法测试完成 → 离线下载/下一步
```

首版关注：

- 可索引页面覆盖率；
- 非品牌查询曝光；
- 各词簇 CTR；
- 工具启动率；
- 测试任务完成率；
- 错误率；
- 离线下载率；
- 自然外链和 GitHub 引用。

不把停留时间直接当作排名指标，也不为了延长时长阻碍用户完成任务。

## 10. 发布计划

### 阶段 0：数据补齐

- 用 Keyword Planner/Ahrefs/Semrush 补充美国英语搜索量、KD、CPC及日期；
- 对每个主词检查 Google 前十和 SERP 模块；
- 实际使用前三个代表性工具并记录不足；
- 检查 Google Trends 5年、12个月和90天；
- 最终决定首版页面是否合并或暂缓。

### 阶段 1：设计与技术原型

- 完成设计系统、移动/桌面原型和全部状态；
- 完成客户端密码学 PoC 和测试向量；
- 完成无秘密采集的分析设计；
- 完成离线单文件构建验证。

### 阶段 2：小批上线

- 上线首页、Generator、Validator、Word List、Checksum、What Is BIP39、Offline 和信任页；
- 提交 GSC 与 Sitemap；
- 只做真实相关分发和开源发布；
- 观察四至八周，不承诺固定收录/排名时间。

### 阶段 3：数据驱动扩展

- 按 GSC 查询补 Converter、Passphrase 或长尾章节；
- 首批模板合格后才扩多语言词表；
- 有真实开发者请求后再做 SDK/白标。

## 11. 决策门槛

新建页面前必须回答：

```text
查询对应什么独立任务？
与现有页的输入/输出/内容差异是什么？
SERP 是否支持这种页面类型？
能否提供独特且可核验的价值？
会不会与现有页面竞争同一意图？
数据是否可靠且可长期维护？
决定：新建 / 合并 / 暂缓 / noindex
```

## 12. 最大风险

1. 安全信任：任何第三方脚本或模糊承诺都可能毁掉品牌。
2. 搜索意图混杂：generator、validator、recovery 和 derivation 不是同一任务。
3. 竞争：Ian Coleman、GitHub 和成熟工具已有强心智，必须以离线验证、解释和体验形成增量。
4. 薄页：语言、词数、币种页面很容易成为模板复制。
5. 商业化弱：消费者工具低频，长期收入需转向联盟与 B2B 组件。
6. 法律与责任：不得保证资产恢复成功，不得暗示通过格式验证即代表钱包安全。

